query notes($offset: Int, $keyword: String, $tag: String){
  notes(offset: $offset, keyword: $keyword, tag: $tag){
      id
      title
      content
      createdAt
      updatedAt
      tags {
        id
        title
      }
  }
}